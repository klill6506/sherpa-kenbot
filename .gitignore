node_modules/
dist/
demo/dist/
*.local
.DS_Store
Thumbs.db
*.log

# Rejected early prototype kept on disk for reference only — not part of the project
Ken Bot/

# Local Claude Code permissions (machine-specific)
.claude/settings.local.json

# Secrets — the ElevenLabs API key lives here for `npm run real-tts`
.env
