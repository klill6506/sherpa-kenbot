# For `npm run real-tts` — the local ElevenLabs proxy used by the demo.
# Copy this file to `.env` and fill in the key. NEVER commit the real .env.
# Get a key: elevenlabs.io -> log in -> avatar menu (bottom-left) -> API Keys.
ELEVENLABS_API_KEY=

# Ken's cloned voice (already the default; override here if needed).
ELEVENLABS_VOICE_ID=Z9CSFTsEQ3J3DsnfCkiX
